# Local PostgreSQL test database.
# Copy to .env.local or set DATABASE_URL in PowerShell before running the app.
DATABASE_URL=postgresql+psycopg://teamazag_app:teamazag_local_pw@localhost:5432/teamazag_local
