export default function HandoffPreview({ previewData, type, onEdit, onRegenerate, onShare, onSave, archived = false }) {
  return <article className="hc-preview"><header className="hc-preview-head"><div><span>{archived ? "ARCHIVED DOCUMENT" : "AI GENERATED PREVIEW"}</span><h2>{previewData.title}</h2></div><span className="hc-status">초안</span></header><div className="hc-preview-body">{previewData.sections.map(([label, values]) => <section className="hc-preview-section" key={label}><h3>{label}</h3><ul>{values.map((value, index) => <li key={`${label}-${index}`}>{value}</li>)}</ul></section>)}</div>{!archived && <footer className="hc-preview-actions"><button type="button" onClick={onEdit}><i className="ti ti-edit" />수정하기</button><button type="button" onClick={() => onRegenerate(type)}><i className="ti ti-refresh" />다시 생성</button><button type="button" onClick={onShare}><i className="ti ti-share" />공유하기</button><button type="button" className="primary" onClick={onSave}><i className="ti ti-device-floppy" />초안 저장</button></footer>}</article>;
}
