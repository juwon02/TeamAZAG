export default function HandoffCandidateList({ candidates, selectedIds, onToggle }) {
  const groups = [...new Set(candidates.map((item) => item.group))];
  return <div className="hc-candidates"><div className="hc-notice"><i className="ti ti-sparkles" /><span>AI가 연결된 업무 자료에서 포함 후보를 추출했습니다. 체크를 해제하면 결과 문서에서 제외됩니다.</span></div>{groups.map((group) => { const items = candidates.filter((item) => item.group === group); return <section className="hc-candidate-group" key={group}><header><h3>{group}</h3><span>{items.filter((item) => selectedIds.includes(item.id)).length}/{items.length}</span></header><div className="hc-candidate-list">{items.map((item) => <label className="hc-candidate" key={item.id}><input type="checkbox" checked={selectedIds.includes(item.id)} onChange={() => onToggle(item.id)} /><span className="hc-checkmark"><i className="ti ti-check" /></span><span className="hc-candidate-copy"><strong>{item.title}</strong><small>{item.meta}</small></span><span className={`hc-priority ${item.priority.toLowerCase()}`}>{item.priority}</span></label>)}</div></section>; })}</div>;
}
